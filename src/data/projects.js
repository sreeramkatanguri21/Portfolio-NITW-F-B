// Static project data has been moved to the Express backend server (server/data/projects.json).
// All project data is now dynamically fetched via REST endpoints (/api/projects and /api/projects/:id).

const projects = [];

export default projects;
